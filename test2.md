# 多无人机协同搜索方案解析

## 一、任务背景与目标

### 1.1 核心任务
在**障碍物环境**下实现多无人机协同搜索，需要同时完成：
- **区域覆盖**：高效覆盖任务区域
- **目标搜索**：对目标进行主动搜索
- **避障导航**：在复杂环境中安全飞行

### 1.2 系统架构
采用**分层架构**（图1）：
- **底层**：引导学习实现端到端策略学习
- **中层**：滚动时域规划框架
- **顶层**：任务分配实现高效搜索

---

## 二、任务环境
### 2.1 环境模型

#### 搜索区域定义
- 矩形平面区域Ω
- 均匀划分为 Lx × Ly 个相同大小的方形网格
- 每个网格Cm的中心位置记为μm

#### 网格离散化
搜索区域被离散化为有限个网格单元,便于:
- 概率图更新
- 路径规划计算
- 覆盖率评估

### 3.2 无人机模型

### 3.2.1 轨迹表示

**多项式表示**（与YOPO一致）：

```
f_μ(t) = a_0 + a_1·t + a_2·t² + a_3·t³ + ... + a_n·t^n, μ∈{x,y}
```

**系数向量**：
```
A = [a_0, a_1, a_2, ..., a_n]  (12维：x和y轴各6个系数)
```

**边界值问题（BVP）**：
```
d = [p_F, v_F, a_F, p_p, v_p, a_p]^T
A = M^(-1) · d = M^(-1) · C · [d_F; d_P]
```

其中：
- `d_F`：固定状态（当前状态）
- `d_P`：自由状态（末端状态，由网络预测）
- `M`：仅与规划时间T有关的映射矩阵

### 2.3 运动基元设计

**基元库定义**（**与YOPO的关键差异**）：

在**二维**情况下（高度固定）：
```
P = {p_0, p_1, p_2, ..., p_N}
p_i = (r·cosθ_i, r·sinθ_i)  ← 仅极角θ，无方位角φ
```

其中 `i ∈ [1,N]`，将视场角均匀切分为N个方向。

**第i条轨迹的细化最终状态可以表示为：**：
```
p'_i = (r'·cosθ'_i, r'·sinθ'_i)
```
其中：
- `θ'_i = θ_i + Δθ_i`（预测偏移）
- `r' = r + Δr`（预测半径偏移）

> **对比YOPO**：YOPO使用3D空间的 `(φ, θ, r)` 参数化，本方案简化为2D的 `(θ, r)`



#### 3.2.2 传感器模型

** μi(k): 地面投影位置 ** 

**覆盖范围**:
- 圆形覆盖区域,半径为Rs
- 覆盖网格集合: SRi(k) = {Cm | ||μm - μi(k)||₂ ≤ Rs}

**检测性能**:
- 检测概率: pd ∈ (0,1)
- 虚警概率: pf ∈ (0,1)

#### 3.2.3 通信模型

**邻居集合**:
```
Ni(k) = {Uj | ||μi(k) - μj(k)||₂ ≤ RC, Uj ∈ U}
```
其中RC为通信范围

**简化假设**:
- 仅考虑有限通信范围
- 忽略带宽限制、通信延迟和中断

### 3.3 认知地图更新

#### 3.3.1 目标存在概率

**符号定义**:
- pim(k): 时刻k无人机Ui对网格Cm中目标存在的概率估计
- θm = 1: Cm中至少存在一个目标
- θm = 0: Cm中不存在目标
- oim(k) = 1: Ui在Cm中检测到目标
- oim(k) = 0: Ui在Cm中未检测到目标

#### 3.3.2 贝叶斯更新

基于传感器观测oim(k)的概率图更新:

```
pim(k) = p(oim(k)|θm=1)pim(k-1) / [p(oim(k)|θm=1)pim(k-1) + p(oim(k)|θm=0)(1-pim(k-1))]
```

具体形式:
- 检测到目标: `pim(k) = pd·pim(k-1) / [pd·pim(k-1) + pf·(1-pim(k-1))]`
- 未检测到目标: `pim(k) = (1-pd)pim(k-1) / [(1-pd)pim(k-1) + (1-pf)(1-pim(k-1))]`
- 未覆盖区域: `pim(k) = pim(k-1)`

#### 3.3.3 非线性变换

为降低计算复杂度,引入对数变换:
```
p̂im(k) = ln(1/pim(k) - 1)
```

转化为线性更新:
```
p̂im(k) = p̂im(k-1) + vim(k)
```

其中:
```
vim(k) = {
  ln(pf/pd),           Cm ∈ SRi(k), oim(k)=1
  ln((1-pf)/(1-pd)),   Cm ∈ SRi(k), oim(k)=0
  0,                   Cm ∉ SRi(k)
}
```

#### 3.3.4 信息融合

基于一致性协议的融合方案:
```
Qim(k) = Σ(j=1 to Nv) wi,j,k · p̂im(k)
```

权重计算:
```
wi,j,k = {
  1/(1+max{|Ni(k)|,|Nj(k)|}),  Uj ∈ Ni(k), j≠i
  1 - Σ wi,t,k,                 Uj ∈ Ni(k), j=i
  0,                            Uj ∉ Ni(k)
}
```

该方法保证所有无人机的概率图收敛到反映目标真实存在性的统一地图。

---

**不确定性**:
```
ηim(k) = e^(-Kη|Qim(k)|)
```
- Kη为正增益参数
- Qim(k)越接近0,不确定性越高

## 三、基于引导学习的协同搜索策略

### 3.1 引导学习核心思想

**动机**：克服传统学习方法的局限
- **模仿学习**：依赖专家质量，上界受限
- **强化学习**：收敛困难，样本效率低

**解决方案**：
> 利用环境物理场的**数值梯度**直接指导神经网络生成最优轨迹

**特权学习机制**：
- **训练阶段**：使用全局完美信息（障碍物ESDF、搜索概率图）
- **推理阶段**：仅凭局部受限观测（传感器数据）

### 3.2 网络架构（图4）

**输入**：
- 环境观测（不确定性图）
- 当前状态（位置、速度、加速度）
- 邻居信息（其他无人机的状态和轨迹）

**输出**（针对每个运动基元）：
- 偏移量：`Δθ`, `Δr`
- 末端导数：速度 `v_p`、加速度 `a_p`
- 得分：`score`

### 3.3 引导损失函数设计

**总损失**：
```
L = L_s(y, -J_g)
```

其中：
- `L_s`：Smooth-L1监督损失（训练得分预测）
- `J_g`：物理场引导损失（四个组成部分）

---

#### 3.3.1 引导搜索项 `J_search`

**目标**：最大化对目标概率密度的采样，并根据信息熵调整速度

```
J_search = -Σ_{k=0}^K ΔtU(p(t_k))
```

其中：
- `U(p)`：环境不确定性

**物理意义**：
- 引导无人机飞向**高不确定性目标区域**

**梯度计算**（数值梯度）：
```
∂J_search/∂d_P = -Σ_k Δt · ∇U(p_k) · ∂p_k/∂d_P
```

---

#### 3.3.2 协同避碰项 `J_coop`

**目标**：在预测时域内实现隐式协同与防碰撞

```
J_coop = Σ_{j∈邻居} Σ_{k=1}^K Δt · exp(-‖p_i(t_k) - p_j(t_k)‖² / 2σ²_safe)
```

其中：
- `p_j(t_k)`：邻居j在时刻t_k的预测位置
- `σ_safe`：安全距离（高斯势场的标准差）

**物理意义**：
- 使用**高斯排斥势场**避免无人机间碰撞
- 距离越近，代价越高（指数增长）
- 基于**预测轨迹**的前瞻性避碰

**梯度计算**：
```
∂J_coop/∂d_P = Σ_j Σ_k (p_i - p_j) / σ²_safe · exp(-‖p_i - p_j‖² / 2σ²_safe) · ∂p_i/∂d_P
```

---

#### 3.3.3 避障项 `J_coll`

**目标**：避免与静态障碍物碰撞

```
J_coll = Σ_{k=1}^K Δt · Φ_obs(p(t_k))
```

其中势能函数：
```
Φ_obs(p) = max(0, D_safe - d(p))²
```

- `d(p)`：到最近障碍物的距离（从ESDF查询）
- `D_safe`：安全阈值

**物理意义**：
- 距离小于`D_safe`时产生**二次惩罚**
- 距离越近，代价增长越快
- 类似于**排斥势能场**

**梯度计算**（数值梯度）：
```
∂J_coll/∂d_P = Σ_k Δt · ∇Φ_obs(p_k) · ∂p_k/∂d_P
              = Σ_k Δt · 2·(D_safe - d(p_k))·(-∇d(p_k)) · ∂p_k/∂d_P
```

其中 `∇d(p_k)` 从ESDF地图查询。

---

#### 3.3.4 动力学平滑项 `J_smooth`

**目标**：确保轨迹满足动力学约束（Minimum Jerk准则）

```
J_smooth = ∫₀ᵀ ‖p⃛(t)‖² dt = A^T · Q · A
```

**边界值形式**：
```
J_smooth = [d_F; d_P]^T · B · [d_F; d_P]
         = d_F^T·B_FF·d_F + 2·d_F^T·B_FP·d_P + d_P^T·B_PP·d_P
```

其中：
```
B = C^T · M^(-T) · Q · M^(-1) · C
```

**解析梯度**：
```
∂J_smooth/∂d_P = 2·d_F^T·B_FP + 2·d_P^T·B_PP
```

**物理意义**：
- 最小化加加速度（jerk）→ 平滑轨迹
- 减少电机磨损和能耗
- 提高飞行舒适度

---

### 3.4 端到端梯度传播

**总梯度**：
```
∂J_g/∂d_P = ∂J_search/∂d_P + ∂J_coop/∂d_P + ∂J_coll/∂d_P + ∂J_smooth/∂d_P
```

**反向传播到网络输出**（链式法则）：

对于**偏移量**：
```
∂J_g/∂y_Δ = (∂J_g/∂d_P) · (∂d_P/∂Δ) · (∂Δ/∂y_Δ)
```

其中：
- `∂d_P/∂Δ`：末端状态对偏移量的雅可比（几何变换）
- `∂Δ/∂y_Δ`：偏移量对网络输出的导数（如 `tanh` 激活函数）

对于**末端导数**：
```
∂J_g/∂y_ε = (∂J_g/∂d_P) · (∂d_P/∂y_ε),  ε∈{v,a}
```

**训练流程**：
1. 网络预测 → 生成轨迹参数 `d_P`
2. 从物理场（ESDF、概率图）查询数值梯度
3. 通过链式法则反向传播到网络权重
4. Adam优化器更新参数

---

## 四、与YOPO的对比分析

| 维度 | YOPO | 本方案 |
|------|------|--------|
| **应用场景** | 单机避障导航 | 多机协同搜索 |
| **空间维度** | 3D（φ, θ, r） | 2D（θ, r，固定高度） |
| **任务目标** | 到达目标点 | 覆盖+搜索+避障 |
| **代价函数** | 平滑+安全+目标 | 搜索+协同+避障+平滑 |
| **协同机制** | 无 | 高斯势场+轨迹预测 |
| **概率地图** | 无 | 贝叶斯目标概率更新 |
| **训练数据** | 预收集深度图+ESDF | 模拟环境+概率图 |

---


### 5 训练数据需求

**特权信息**（训练时）：
- 完美障碍物ESDF地图
- 真实目标位置（生成概率图）
- 所有无人机的完整状态

**推理时输入**（仅需）：
- 局部传感器观测（有限视野、噪声）
- 当前状态估计
- 邻居通信信息
