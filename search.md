# AAV集群协同搜索：任务建模与方法论详细分析

## 论文基本信息
- **标题**: 基于可扩展多智能体深度强化学习与数字孪生使能的仿真到现实迁移的AAV集群协同搜索
- **作者**: 曹攀, 雷磊, 沈高清, 蔡盛锁, 刘晓娇, 刘晓昌
- **发表**: IEEE Transactions on Mobile Computing, 2025年6月, 第24卷第6期
- **单位**: 南京航空航天大学

---

## 第一部分：任务建模

### 1.1 问题定义

**协同目标搜索(CTS)问题**：在动态威胁环境中，多个自主飞行器(AAV)协同搜索未知目标信号源。

**核心挑战**：
- 部分可观测性：AAV只能感知有限范围内的信息
- 动态环境：威胁和目标位置未知且可能变化
- 通信受限：AAV之间的通信能力有限
- 实时性要求：需要快速做出决策
- 可扩展性：算法需支持不同规模的AAV集群

### 1.2 环境模型

#### 1.2.1 搜索区域建模

**离散化网格空间**：
- 搜索区域 Ω 被划分为 **Lx × Ly** 个等大的离散网格
- 每个网格中心坐标：**cx,y = (x, y)**，其中 x ∈ {1, 2,...,Lx}, y ∈ {1, 2,...,Ly}
- 网格状态表示：
  - **τx,y = 1**: 网格中存在目标
  - **τx,y = 0**: 网格中无目标

**目标分布**：
- 使用**伯努利分布**建模目标存在概率
- 网格 Cx,y 中存在目标的概率为 **Px,y**
- 不存在目标的概率为 **1 - Px,y**
- 总共有 **M** 个未知目标信号源：M = {1, 2,...,M}
- 每个目标向所有方向辐射信号

**威胁建模**：
- 动态威胁数量：**No** 个
- 威胁位置坐标：**zk = (x, y)**，其中 1 ≤ k ≤ No
- 威胁在搜索区域内随机分布

#### 1.2.2 AAV模型

**集群配置**：
- AAV集群：**U = {U1, U2,...,Ui,...,UNu}**
- 集群规模：**Nu** 个AAV
- 任务周期：离散化为 **T** 个时间步
- AAV在每个时间步开始时做出移动决策

**位置表示**：
- AAV i 在时间步 t 的坐标：**ui,t = (x, y)**
- 位置通过投影到任务区域 Ω 上来识别

**动作空间**：
每个AAV有5个可能的移动方向：
- **动作0**: 向上
- **动作1**: 向下
- **动作2**: 向左
- **动作3**: 向右
- **动作4**: 静止

**运动约束**：

考虑硬件性能限制，AAV的运动方向不能在连续两个时间步内任意切换。

**最大转向角约束**：
```
arccos(vi,t · vi,t+1 / |vi,t| |vi,t+1|) ≤ ψ
```

其中：
- **vi,t**: 当前时间步的速度矢量
- **vi,t+1**: 下一时间步的速度矢量
- **ψ**: 最大转向角

#### 1.2.3 传感器模型

**检测设备**：场强计(Field Strength Meter)

**信号强度计算**：

每个方向接收到的信号强度由**自由空间路径损耗模型**计算：

```
Pr = Pt + Gt + Gr + 20lg(λ/4π) - 20lg(d)
```

参数说明：
- **Pr**: 接收功率 (dBm)
- **Pt**: 发射功率 (dBm)
- **Gt**: 发射天线增益 (dBi)
- **Gr**: 接收天线增益 (dBi)
- **λ**: 波长 (m)
- **d**: 距离 (m)

**四向天线检测**：

场强计返回四个方向源的信号强度，最终检测信号强度取最大值：

```
Pr,max = max(Pr1, Pr2, Pr3, Pr4)
```

其中 Pr1, Pr2, Pr3, Pr4 分别是四个方向的接收信号强度。

**观测结果建模**：

AAV i 在时间步 t 对网格 Cx,y 的观测结果记为 **Zi,t_x,y**：
- **Zi,t_x,y = 1**: 在网格 Cx,y 检测到目标
- **Zi,t_x,y = 0**: 在网格 Cx,y 未检测到任何东西

**传感器条件概率**：

传感器模型的正负检测条件概率表示为：

```
P(Zi,t_x,y | τx,y) = {
    P(Zi,t_x,y = 1 | τx,y = 1) = p      (正确检测)
    P(Zi,t_x,y = 0 | τx,y = 1) = 1-p    (漏检)
    P(Zi,t_x,y = 1 | τx,y = 0) = q      (虚警)
    P(Zi,t_x,y = 0 | τx,y = 0) = 1-q    (正确排除)
}
```

参数含义：
- **p**: 真检测概率
- **1-p**: 漏检概率
- **q**: 虚警概率
- **1-q**: 真排除概率

#### 1.2.4 概率图更新机制

**个体概率图维护**：

每个AAV i 维护任务区域的个体概率图：
```
Pi,t = {Pi,t_x,y | τx,y = 1}
```

这是实现完全分布式决策方法的关键。

**贝叶斯更新规则**：

标准贝叶斯更新公式：

```
Pi,t+1_x,y = {
    (p·Pi,t_x,y) / (p·Pi,t_x,y + q·(1-Pi,t_x,y)),           if Zi,t_x,y = 1
    ((1-p)·Pi,t_x,y) / ((1-p)·Pi,t_x,y + (1-q)·(1-Pi,t_x,y)), if Zi,t_x,y = 0
    Pi,t_x,y,                                                 otherwise
}
```

**线性化更新方法**：

为便于计算，采用对数几率变换的线性更新方式。

首先，标准贝叶斯公式等价于：

```
1/Pi,t+1_x,y - 1 = {
    (q/p) · (1/Pi,t_x,y - 1),           if Zi,t_x,y = 1
    ((1-q)/(1-p)) · (1/Pi,t_x,y - 1),   if Zi,t_x,y = 0
    1/Pi,t_x,y - 1,                     otherwise
}
```

**对数几率变换**：

引入非线性变换：
```
Ψi,t_x,y ≜ ln(1/Pi,t_x,y - 1)
```

这样，更新公式变为**线性形式**：

```
Ψi,t+1_x,y = Ψi,t_x,y + εi,t_x,y
```

其中更新增量为：

```
εi,t_x,y = {
    ln(q/p),         if Zi,t_x,y = 1  (检测到目标)
    ln((1-q)/(1-p)), if Zi,t_x,y = 0  (未检测到)
    0,               otherwise         (未观测)
}
```

**优势**：
- 计算效率高：避免了除法运算
- 数值稳定性好：对数空间操作
- 便于信息融合：线性叠加

### 1.3 问题形式化

#### 1.3.1 优化目标

**目标1：最大化目标发现数量**

```
f1 = Σ(t=1到T) Σ(x=1到Lx) Σ(y=1到Ly) Δ(Pt_x,y)
```

其中指示函数：
```
Δ(Pt_x,y) = {
    1, if Pt_x,y ≥ ζ  (目标存在概率超过阈值)
    0, else           (概率低于阈值)
}
```
- **Pt_x,y**: 网格的全局目标存在概率
- **ζ**: 预定义阈值（如ζ=0.9表示90%置信度）

**目标2：最大化区域覆盖率**

```
f2 = Σ(t=1到T) Σ(x=1到Lx) Σ(y=1到Ly) Δ(Nvisited_x,y)
```

其中：
```
Δ(Nvisited_x,y) = {
    1, if Nvisited_x,y > 0  (网格已被访问)
    0, else                 (网格未被访问)
}
```
- **Nvisited_x,y**: 网格 Cx,y 的访问次数

#### 1.3.2 安全约束

**约束1：AAV间防碰撞**

```
||ui,t - uj,t|| > dsafe,1,  ∀Uj ∈ Hi
```

- **ui,t, uj,t**: AAV i 和 j 的位置
- **dsafe,1**: AAV间的安全距离
- **Hi**: AAV i 的邻居集合

**约束2：威胁规避**

```
||ui,t - zk|| > dsafe,2,  ∀k ∈ {1, 2,...,No}
```

- **zk**: 威胁 k 的位置
- **dsafe,2**: 与威胁的安全距离

#### 1.3.3 完整优化问题

**多目标约束优化问题**：

```
max f = [f1, f2]

subject to:
    1 ≤ ui,t(x) ≤ Lx                              (区域边界约束)
    1 ≤ ui,t(y) ≤ Ly                              (区域边界约束)
    arccos(vi,t·vi,t+1 / ||vi,t|| ||vi,t+1||) ≤ ψ (转向角约束)
    ||ui,t - uj,t|| > dsafe,1,  ∀Uj ∈ Hi          (AAV间安全)
    ||ui,t - zk|| > dsafe,2,  ∀k ∈ {1,2,...,No}   (威胁规避)
```

**计算复杂性分析**：

该多目标优化问题的计算复杂度随AAV规模呈**指数增长**，传统优化技术难以实时求解。因此，本文采用基于多智能体深度强化学习(MADRL)的方法。

### 1.4 Dec-POMDP框架

**部分可观测马尔可夫决策过程(Dec-POMDP)**建模：

CTS问题可形式化为一个Dec-POMDP，定义为七元组：

```
<N, S, A, O, T, R, γ>
```

**要素说明**：

1. **N**: 智能体数量（AAV数量 Nu）

2. **S**: 全局状态空间
   - 包含所有AAV位置、目标位置、威胁位置
   - **st ∈ S**: 时间步 t 的全局状态

3. **A**: 联合动作空间
   - **A = A1 × A2 × ... × ANu**
   - **ai,t ∈ Ai**: AAV i 在时间 t 的动作
   - **at = (a1,t, a2,t,...,aNu,t)**: 联合动作

4. **O**: 观测空间
   - **oi,t ∈ Oi**: AAV i 的局部观测
   - 通过观测函数生成：**To(ai,t-1, st): A × S → O**
   - 指定概率：**P(ot | at-1, st)**

5. **T**: 状态转移函数
   - **T(st+1 | st, at): S × A → S**
   - 给定当前状态和联合动作，确定下一状态

6. **R**: 奖励函数
   - **R(st, at): S × A → ℝ**
   - 评估当前状态-动作对的质量

7. **γ**: 折扣因子
   - **γ ∈ [0, 1)**
   - 平衡即时和长期奖励

**策略定义**：

联合策略 **π: S → A** 定义了从状态到动作的映射关系。

**MADRL目标**：

学习联合策略 π，最大化累积折扣奖励：

```
J(π) = 𝔼[Σ(t=0到∞) γ^t · R(st, at)]
```

---

## 第二部分：方法论详解

### 2.1 观测空间设计

#### 2.1.1 视野范围(FOV)策略

**设计动机**：
- 如果直接使用整个任务区域的信息图作为智能体输入，会包含大量无效信息
- 无效信息会减慢训练速度，降低决策效率
- 需要优化状态空间设计

**解决方案**：
采用**视野范围(Field-of-View, FOV)**机制：
- 每个AAV只提取其FOV范围内的局部信息
- 大幅减少输入维度
- 加速神经网络训练

#### 2.1.2 图表示方法

**可扩展性设计**：

采用**图表示(Graph Representation)**构建可扩展的观测空间：
- 提高在不同规模AAV集群任务中的泛化能力
- 支持动态数量的邻居AAV
- 实现规模无关的模型架构

**观测提取过程**：

每个时间步，AAV从其FOV内提取局部信息作为智能体网络输入，帮助确定下一步动作。

#### 2.1.3 环境认知图

从环境认知图中提取的信息分为**四个部分**：

**部分1：目标存在概率图 (ρ1)**

```python
ρ1,i = 提取自AAV局部概率图的FOV窗口
```

特点：
- 从AAV的局部概率图中提取
- 通过信息共享更新
- FOV边界外的目标存在概率假设为0
- 尺寸：通常为 7×7 或 9×9 网格

**部分2：访问次数信息图 (ρ2)**

```python
ρ2,i = FOV内每个网格的访问次数
```

特点：
- 如果网格在某个时间步被AAV访问，访问次数加1
- FOV边界外的访问次数假设为0
- 帮助AAV避免重复搜索已探索区域

**部分3：邻居与威胁信息图 (ρ3)**

```python
ρ3,i = {
    0.5,  FOV内的威胁位置
    1.0,  FOV内的其他AAV位置
    0,    空网格或FOV边界外
}
```

特点：
- 提供碰撞规避所需的局部态势感知
- 区分威胁和友军
- 支持协同避障

**部分4：AAV自身位置 (p4)**

```python
p4,i = [x坐标, y坐标] 或归一化坐标
```

特点：
- 添加到观测向量中
- 帮助AAV了解自身状态
- 可能包括速度、方向等额外信息

**完整观测空间**：

```
Oi = [ρ1,i, ρ2,i, ρ3,i, p4,i]
```

**维度分析**（假设FOV为7×7）：
- ρ1: 7×7 = 49 维
- ρ2: 7×7 = 49 维
- ρ3: 7×7 = 49 维
- p4: 2 维（x, y坐标）
- **总计**: 49+49+49+2 = **149维**

相比全局信息（如50×50=2500维），大幅降低了输入维度。

#### 2.1.4 动作空间

**可用动作**：

基于当前位置，AAV可选择最多5个动作：

```
A = {0:北, 1:南, 2:东, 3:西, 4:静止}
```

**动作过滤**：
- 任何会导致AAV在下一步离开边界的移动都会从选项中排除
- 例如：如果AAV在北边界，则动作"北"不可用

**转向约束**：
- 考虑最大转向角 ψ
- 某些急转弯动作可能被限制

### 2.2 奖励函数设计

奖励函数对复杂环境中的AAV决策行为有重要影响。本文设计了具有**三个主要目标**的奖励方案：
1. 尽快发现更多未知目标
2. 最小化环境不确定性
3. 避免与邻近AAV和威胁碰撞

#### 2.2.1 探索认知奖励 (Exploration Cognition Reward)

**目的**：引导AAV探索未探索的任务区域，特别是目标存在高概率的区域，同时减少环境不确定性。

**公式**：

```
rcog,t = α · Σ(x=1到Lx) Σ(y=1到Ly) (Ht-1_x,y - Ht_x,y)
```

**信息熵定义**：

```
Ht_x,y = -Pt_x,y·log(Pt_x,y) - (1-Pt_x,y)·log(1-Pt_x,y)
```

**参数说明**：
- **α**: 正常数，权重系数（如 α=1.0）
- **Ht_x,y**: 网格在时间t的信息熵
- **Pt_x,y**: 网格中目标存在的全局概率

**工作原理**：
- 信息熵衡量不确定性：熵越高，不确定性越大
- 当AAV观测某网格后，该网格的熵降低
- 奖励等于熵的减少量，鼓励降低环境不确定性
- 当 Pt_x,y 接近0或1时，熵最小（确定性最高）
- 当 Pt_x,y 接近0.5时，熵最大（不确定性最高）

#### 2.2.2 目标发现奖励 (Target Found Reward)

**目的**：协同目标搜索任务的主要目标是发现更多目标。因此，在时间t发现新目标时，给予AAV i 正奖励。

**公式**：

```
rtar,t = β · Σ(i=1到Nu) Σ(x=1到Lx) Σ(y=1到Ly) 𝟙{Pi,t-1_x,y < ξ ≤ Pi,t_x,y 且 Pj,t_x,y < ξ, ∀j≠i}
```

**参数说明**：
- **β**: 正常数，目标发现奖励权重（如 β=10.0）
- **ξ**: 目标确认阈值（如 ξ=0.9，即90%置信度）
- **𝟙{·}**: 指示函数，条件满足时为1，否则为0

**触发条件**：
1. 时间t-1时，AAV i认为该网格目标概率低于阈值：**Pi,t-1_x,y < ξ**
2. 时间t时，AAV i认为该网格目标概率达到或超过阈值：**Pi,t_x,y ≥ ξ**
3. 其他所有AAV j在时间t仍认为概率低于阈值：**Pj,t_x,y < ξ, ∀j≠i**

**设计意图**：
- 奖励首次发现目标的AAV
- 避免重复奖励同一目标
- 促进AAV分散搜索不同区域

#### 2.2.3 时间消耗奖励 (Time Consumption Reward)

**目的**：引导AAV在最短时间内完成搜索任务。

**公式**：

```
rtime,t = -λ
```

**参数说明**：
- **λ**: 正常数，时间惩罚系数（如 λ=0.1）

**工作原理**：
- 每个时间步都施加固定的负奖励
- 激励AAV高效完成任务
- 避免无效移动和时间浪费
- 总时间消耗惩罚：**-λ·T**（T为任务完成时间）

#### 2.2.4 区域覆盖奖励 (Area Coverage Reward)

**目的**：引导AAV覆盖更多未探索的网格单元。

**公式**：

```
rcov,t = μ · Σ(i=1到Nu) (Nnew,t,i / Stot)
```

**参数说明**：
- **μ**: 正常数，用于缩放奖励（如 μ=1.0）
- **Nu**: AAV数量
- **Nnew,t,i**: AAV i在时间步t新覆盖的网格单元数量
- **Stot**: 整个区域的网格单元总数

**计算方法**：
```
Nnew,t,i = |{(x,y) | Nvisited_x,y(t) = 1 且 Nvisited_x,y(t-1) = 0}|
```

**归一化**：
- 除以总网格数Stot进行归一化
- 使奖励在不同规模任务中具有可比性
- 值域：[0, 1]

**协同考虑**：
- 累加所有AAV的贡献
- 鼓励集群整体的覆盖效率
- 避免AAV聚集在同一区域

#### 2.2.5 协同安全奖励 (Collaboration Safety Reward)

**目的**：引导AAV远离威胁和其他AAV，确保安全飞行。

**公式**：

```
rsafe,t = -γ · Σ(i=1到Nu-1) Σ(j=i+1到Nu) 𝟙{||ui,t - uj,t|| ≤ dsafe,1}
          -η · Σ(i=1到Nu) Σ(k=1到No) 𝟙{||ui,t - zk|| ≤ dsafe,2}
```

**参数说明**：
- **γ**: AAV间碰撞惩罚系数（如 γ=5.0）
- **η**: 威胁碰撞惩罚系数（如 η=10.0）
- **dsafe,1**: AAV间安全距离（如 dsafe,1=2个网格单元）
- **dsafe,2**: 与威胁的安全距离（如 dsafe,2=3个网格单元）

**两部分惩罚**：

1. **AAV间碰撞惩罚**（第一项）：
   - 双重求和避免重复计数（i<j）
   - 当两个AAV距离小于安全距离时触发
   - 惩罚值：-γ（每对碰撞的AAV）

2. **威胁碰撞惩罚**（第二项）：
   - 遍历所有AAV和所有威胁
   - 当AAV距离威胁小于安全距离时触发
   - 惩罚值：-η（每个进入危险区的AAV）

**设计考虑**：
- 通常 η > γ（威胁更危险）
- 负奖励强度可调，平衡安全与效率
- 软约束，与硬约束（物理碰撞检测）配合使用

#### 2.2.6 总体奖励函数

**完整奖励函数**：

```
rt = rcog,t + rtar,t + rtime,t + rcov,t + rsafe,t
```

**展开形式**：

```
rt = α·Σ(Ht-1_x,y - Ht_x,y) 
     + β·[新发现目标数] 
     - λ 
     + μ·Σ(Nnew,t,i/Stot) 
     - γ·[AAV碰撞数] 
     - η·[威胁碰撞数]
```

**权重平衡**：

典型参数设置（需根据具体任务调整）：
- **α = 1.0**: 探索认知基础权重
- **β = 10.0**: 目标发现高权重（主要目标）
- **λ = 0.1**: 时间惩罚小权重（避免过度激进）
- **μ = 1.0**: 覆盖奖励中等权重
- **γ = 5.0**: AAV碰撞中等惩罚
- **η = 10.0**: 威胁碰撞高惩罚（安全优先）

**多目标权衡**：
- 奖励函数平衡了探索与利用
- 效率与安全的权衡
- 个体目标与集体目标的协调

### 2.3 SAMARL算法详解

#### 2.3.1 算法概述

**SAMARL** (Scalable Attention-based Multi-Agent Reinforcement Learning) 是基于近端策略优化(PPO)的扩展算法，有效解决了多智能体协同挑战。

**核心特点**：
1. 基于策略梯度方法
2. 采用重要性采样提高数据利用率
3. 使用裁剪操作确保策略稳定性
4. 集成多头注意力机制增强协同
5. CTDE（集中训练分散执行）范式

#### 2.3.2 策略梯度基础

**策略表示**：

策略 πθ 以参数 θ 表征，接收环境输入s，输出动作的概率分布：

```
πθ(a|s): S → Δ(A)
```

其中 Δ(A) 表示动作空间A上的概率分布。

**采样与执行**：
1. 从分布中采样动作：**a ~ πθ(·|s)**
2. 在环境中执行动作a
3. 接收奖励r和下一状态s'

**长期累积奖励**：

```
J(πθ) = 𝔼τ~πθ [Σ(t=0到T) γ^t·rt]
```

其中 τ 表示智能体的轨迹。

**策略梯度定理**：

梯度更新规则为：

```
∇θ J(πθ) = 𝔼τ~πθ [∇θ log πθ(a|s) · Aπθ(s,a)]
```

**优势函数 Aπθ**：

使用**广义优势估计(GAE)**计算：

```
Aπθ(s,a) = Σ(l=0到∞) (γλ)^l · (Rt+l+1 - Vφ(st+l))
```

参数说明：
- **γ**: 折扣因子（如 γ=0.99）
- **λ**: GAE权重因子（如 λ=0.95）
- **Rt+l+1**: 时间t+l+1的目标奖励
- **Vφ(s)**: 状态价值估计（由评论家网络提供）

**GAE的优势**：
- 平衡偏差和方差
- λ=0：高偏差，低方差（TD(0)）
- λ=1：低偏差，高方差（蒙特卡洛）
- λ∈(0,1)：折中方案

#### 2.3.3 重要性采样

**数据利用效率问题**：

在策略梯度方法中，标准方法只能使用由最新策略采样的数据，导致：
- 样本效率低
- 需要大量环境交互
- 训练速度慢

**重要性采样解决方案**：

引入重要性采样技巧，使用先前策略采样的数据。

假设：
- 旧策略：**πθ'**（用于采样数据）
- 新策略：**πθ**（当前优化的策略）

**概率比率**：

```
r(a|s) = πθ(a|s) / πθ'(a|s)
```

**修正后的梯度**：

```
∇θ'→θ J(πθ) = 𝔼τ~πθ' [r(a|s) · Aπθ'(s,a) · ∇θ log πθ(a|s)]
```

**目标函数**：

```
J(θ) = 𝔼τ~πθ' [r(a|s) · Aπθ'(s,a)]
```

**问题**：
- 如果新旧策略分布差异过大，梯度估计不准确
- 可能导致策略更新过大，训练不稳定

#### 2.3.4 PPO裁剪机制

**约束策略更新**：

为确保新旧策略分布差异不太大，SAMARL采用**裁剪操作**。

**Actor网络损失函数**：

```
L(θ) = 𝔼τ~πθ' [min(
    r(oi,ai)·Aπθ',
    clip(r(oi,ai), 1-ε, 1+ε)·Aπθ'
)]
```

**裁剪函数**：

```
clip(r, 1-ε, 1+ε) = {
    1-ε,  if r < 1-ε
    r,    if 1-ε ≤ r ≤ 1+ε
    1+ε,  if r > 1+ε
}
```

**参数说明**：
- **ε**: 裁剪超参数（如 ε=0.2，即±20%）
- **r(oi,ai)**: 动作概率比率
- **Aπθ'**: 优势函数值

**工作原理**：

1. **正优势** (Aπθ' > 0，好的动作)：
   - 未裁剪：鼓励增加该动作概率
   - 裁剪上界：限制增加幅度不超过(1+ε)

2. **负优势** (Aπθ' < 0，坏的动作)：
   - 未裁剪：鼓励减少该动作概率
   - 裁剪下界：限制减少幅度不超过(1-ε)

**悲观下界**：
- min操作选择较小值
- 防止策略过度更新
- 提供保守的策略改进

#### 2.3.5 Critic网络

**Critic网络角色**：
- 参数化为 Vφ（参数φ）
- 估计状态价值函数
- 为Actor提供基线，计算优势函数

**训练目标**：

最小化损失函数：

```
L(φ) = (1/BN) · Σ(i=1到B) Σ(k=1到N) max(
    [Vφ(sk_i) - R̂i]^2,
    [clip(Vφ(sk_i), Vφ'(sk_i)-ε, Vφ'(sk_i)+ε) - R̂i]^2
)
```

**参数说明**：
- **B**: 批次大小
- **N**: AAV数量
- **sk_i**: 智能体i在时间k的状态
- **R̂i**: 目标回报（实际累积奖励）
- **Vφ(sk_i)**: 当前价值估计
- **Vφ'(sk_i)**: 旧价值估计
- **ε**: 裁剪参数（与Actor相同）

**裁剪价值函数**：

```
clip(Vφ, Vφ'-ε, Vφ'+ε) = {
    Vφ'-ε,  if Vφ < Vφ'-ε
    Vφ,     if Vφ'-ε ≤ Vφ ≤ Vφ'+ε
    Vφ'+ε,  if Vφ > Vφ'+ε
}
```

**设计意图**：
- 防止价值函数估计变化过大
- 保持训练稳定性
- 悲观更新（取max，选较大误差）

#### 2.3.6 CTDE框架

**集中训练(Centralized Training)**：

在训练阶段：
- Critic网络基于**全局状态**评估每个AAV的Actor网络
- 可以访问所有AAV的状态和动作信息
- 学习全局价值函数：**V(s)** 或 **Q(s,a)**

**分散执行(Decentralized Execution)**：

在执行阶段：
- 每个AAV使用其**Actor网络**基于**局部观测**做出实时行为决策
- 无需全局状态信息
- 只需与邻居AAV通信

**优势**：
- 训练时利用全局信息，学习更好的策略
- 执行时分布式决策，降低通信需求
- 可扩展性强，适合大规模系统

**信息流**：

训练时：
```
全局状态 st → Critic → 价值评估 V(st)
局部观测 oi,t → Actor i → 动作 ai,t
```

执行时：
```
局部观测 oi,t → Actor i → 动作 ai,t
(不需要Critic和全局状态)
```

#### 2.3.7 多头注意力机制

**动机**：

随着AAV数量增加，模型复杂性显著提高，导致：
- 传统全连接网络性能下降
- 收敛速度慢
- 难以处理可变数量的邻居

**解决方案**：

SAMARL将**多头注意力机制**集成到Actor网络中，使智能体能够关注来自邻居的更重要观测数据。

**注意力计算**：

集合意图通过基于置信度水平的建议加权聚合：

```
zi = Σ(j=1到N) [exp(fu·qi·lj / √dx) · vj] / Σ(n=1到N) exp(fu·qi·ln / √dx)
```

**符号说明**：

- **fu**: 除当前智能体i外其他智能体的特征
- **qi**: 当前智能体i的查询(Query)
- **lj**: 智能体j的认知(Key)
- **vj**: 智能体j的建议(Value)
- **dx**: 缩放系数（通常为嵌入维度的平方根）
- **zi**: 智能体i的集合意图(Collective Intention)

**Query、Key、Value**：

这些都是局部观测 oi 的线性投影：

```
qi = Wq · oi
lj = Wk · oj
vj = Wv · oj
```

其中 Wq, Wk, Wv 是可学习的权重矩阵。

**置信度(Attention Score)**：

```
αij = exp(fu·qi·lj / √dx) / Σ(n=1到N) exp(fu·qi·ln / √dx)
```

- 点积 qi·lj 衡量智能体i对智能体j的关注程度
- 除以√dx 防止点积过大
- Softmax归一化得到注意力权重

**工作原理**：
1. 每个邻居AAV j提供一个"认知"lj和"建议"vj
2. 当前AAV i计算与每个邻居的匹配度（qi·lj）
3. 匹配度高的邻居获得更高权重
4. 加权求和得到集合意图zi

**多头扩展**：

实际实现中使用多个注意力头：

```
zi = Concat(head1, head2, ..., headH) · Wo
```

每个头关注不同的特征子空间，增强表达能力。

#### 2.3.8 最终动作值确定

**集合意图与个体信息融合**：

```
Vt^i = MLP(ht^i, f(zt^i))
```

**参数说明**：
- **Vt^i**: 智能体i在时间t的动作值
- **ht^i**: 智能体i的历史信息
- **zt^i**: 集合意图（来自注意力机制）
- **f(·)**: 全连接神经网络层
- **MLP**: 多层感知器

**历史信息提取**：

使用**门控循环单元(GRU)**处理时序信息：

```
ht^i = GRU(ot^i, ht-1^i)
```

- **ot^i**: 当前时间步的观测
- **ht-1^i**: 上一时间步的隐状态
- **ht^i**: 当前隐状态（编码历史信息）

**GRU的作用**：
- 捕捉时间依赖性
- 记忆重要的历史信息
- 处理部分可观测性问题

**网络架构总结**：

```
观测 oi → GRU → 历史特征 hi
            ↓
观测 oi → Attention → 集合意图 zi
            ↓
[hi, zi] → MLP → 动作概率分布 πθ(·|oi)
```

#### 2.3.9 算法伪代码解析

**主要步骤**：

```
算法1：SAMARL（针对每个DT线程）

输入：
    - 搜索环境孪生 Env
    - AAV、目标、障碍物初始位置
    - 最大训练步数 stepmax
    - 数据缓冲区 D

初始化：
    - Actor网络参数 θ
    - Critic网络参数 φ

for step < stepmax:
    清空数据缓冲区 D = {}
    同步所有DT的参数 θ 和 φ
    接收初始状态 s0 和每个智能体的观测 oi,0
    
    for 批次 b = 1 to B:
        初始化轨迹 τ = []
        
        for 时间步 t = 1 to T:
            for 每个AAV i = 1 to N:
                # 获取观测
                oi,t = O(st, i)
                
                # 计算注意力组件
                计算 {qi,t, ki,t, vi,t}
                
                # 信息交换
                发送 {ki,t, vi,t} 给其他AAV
                接收 {k1:N,t, v1:N,t} 从其他AAV
                
                # 计算集合意图
                zi,t = Attention(qi,t, k1:N,t, v1:N,t)
                
                # 更新历史信息
                ht^i = GRU(oi,t, ht-1^i)
                
                # 计算价值
                vt^i = V^i(zt^i, ht^i)
                
                # 生成动作概率
                pt^i = π(zt^i, ht^i; θ)
                
                # 采样动作
                at^i ~ pt^i
            
            # 执行联合动作
            执行 at，获得 rt, st+1, ot+1
            
            # 记录轨迹
            τ += [st, ot, ht, π, ht, V, at, rt, st+1, ot+1]
        
        # 计算优势函数
        A = GAE(τ)
        
        # 计算回报
        R = ComputeReturns(τ)
        
        # 存储数据
        D = D ∪ (τ, A, R)
    
    # 策略更新
    for mini-batch h = 1 to H:
        从D中采样B条数据
        
        # 更新Actor
        用Adam优化器最小化 L(θ)
        
        # 更新Critic
        用Adam优化器最小化 L(φ)

输出：
    训练好的Actor网络 πθ 和 Critic网络 Vφ
```

**关键点**：

1. **多线程并行**：每个DT线程独立运行，提高采样效率

2. **信息交换**：AAV之间交换Key和Value，实现协同

3. **双网络更新**：Actor和Critic分别优化

4. **批量训练**：收集B个完整轨迹后统一更新

5. **迭代更新**：对同一批数据进行H次mini-batch更新

### 2.4 数字孪生训练框架

#### 2.4.1 框架总体架构

**"孪生训练、分散执行、持续演化"**范式：

该框架将数字孪生技术与MADRL算法集成，执行协同目标搜索任务。

**四大组成部分**：

```
┌─────────────────────────────────────────┐
│          数字孪生训练框架              │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────┐      ┌──────────────┐ │
│  │  物理实体   │◄────►│  决策模型    │ │
│  │  Physical   │      │  Decision    │ │
│  │  Entity     │      │  Model       │ │
│  └─────────────┘      └──────────────┘ │
│         ▲                     ▲         │
│         │                     │         │
│         ▼                     ▼         │
│  ┌─────────────┐      ┌──────────────┐ │
│  │  数字模型   │◄────►│  仿真中间件  │ │
│  │  Digital    │      │  Simulation  │ │
│  │  Model      │      │  Middleware  │ │
│  └─────────────┘      └──────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

#### 2.4.2 物理实体 (Physical Entity)

**组成**：
- AAV集群系统
- 任务环境

**AAV配置**：
- **传感器**：场强计，用于感知环境状态和检测目标信号源
- **计算能力**：有限（Raspberry Pi）
  - 足够支持局部搜索决策
  - 不足以支持MADRL决策模型的有效训练
- **通信模块**：5G模块 + 通信电台

**限制**：
- 机载计算资源有限
- 实时性要求高
- 多AAV协同计算负担重

**解决方案**：
- 在中央服务器上进行模型训练
- AAV仅执行推理（前向传播）

#### 2.4.3 数字模型 (Digital Model)

**定义**：物理实体的超高保真镜像。

**建模技术**：
- 仿真建模技术
- 物理引擎（3D）
- 通信网络仿真

**孪生对象**：
1. **AAV孪生模型**：
   - 飞行动力学模型
   - 传感器特性模型
   - 通信模型

2. **目标孪生模型**：
   - 信号辐射特性
   - 位置分布

3. **环境孪生模型**：
   - 地形地貌
   - 威胁分布
   - 气象条件（可选）

**并行架构**：

中央服务器构建**Nd个数字孪生**实例：

```
DT1: 场景1运行
DT2: 场景2运行
...
DTNd: 场景Nd运行
```

**独立性**：
- 每个DT独立运行
- 相互之间不影响
- 可以设置不同的场景参数

**优势**：
- 提高样本数据获取效率
- 并行探索不同策略
- 加速模型训练

#### 2.4.4 决策模型 (Decision Model)

**核心**：协同目标搜索决策模型（SAMARL）。

**功能**：
- 计算和控制中心
- 接收DT环境的状态信息
- 生成AAV的动作指令

**孪生训练过程**：

```
1. 构建多个数字孪生场景
   └─> Nd个并行DT环境

2. 从DT中采样数据
   └─> 高效、多样的训练样本

3. 训练SAMARL模型
   └─> Actor和Critic网络

4. 评估性能
   └─> 在DT中测试

5. 迭代优化
   └─> 重复步骤2-4
```

**持续演化**：
- 在任务执行期间，如果DT系统保持连接
- 决策模型可获取实时数据
- 进行持续训练和优化
- 物理实体在后续任务周期更新本地模型

#### 2.4.5 仿真中间件 (Simulation Middleware)

**作用**：连接CTS物理实体与各种仿真组件。

**功能**：
1. **数据封装**：统一数据格式
2. **信息转发**：在分布式系统中传递信息
3. **实时数据交换**：支持物理实体与数字模型间的双向通信
4. **模型部署**：中央服务器通过中间件将智能决策模型部署到物理AAV

**通信协议**：
- MAVLink（AAV ↔ 地面站）
- 自定义协议（AAV ↔ AAV）
- HTTP/WebSocket（AAV ↔ DT服务器）

**接口**：
```
物理AAV ←→ 中间件 ←→ 数字模型
                ↕
           决策模型
```

#### 2.4.6 训练阶段

**孪生训练**：

1. **场景生成**：
   - 同时创建Nd个数字孪生环境
   - 每个环境参数随机化（目标位置、威胁分布等）

2. **并行采样**：
   - 所有DT同时运行
   - 每个DT独立采样轨迹数据
   - 汇总到中央数据缓冲区

3. **模型训练**：
   - 使用采样数据训练SAMARL
   - 更新Actor和Critic网络参数
   - 周期性同步参数到所有DT

4. **性能评估**：
   - 在测试DT环境中评估策略
   - 记录关键指标（奖励、目标发现数、碰撞次数等）

**优势分析**：

| 指标 | 无DT | 使用DT (Nd=32) | 改进 |
|------|------|----------------|------|
| 训练时间 | 880单位 | 100单位 | **-88.62%** |
| 样本多样性 | 低 | 高 | 显著提升 |
| 探索效率 | 慢 | 快 | 更快降低熵 |

**训练曲线特点**：
- 更快收敛
- 更高的最终性能
- 更稳定（波动小）

#### 2.4.7 执行阶段

**分散执行**：

1. **模型部署**：
   - 将训练好的Actor网络部署到物理AAV
   - 每个AAV独立运行推理

2. **分布式决策**：
   - 基于局部观测
   - 无需中央控制
   - 与邻居AAV通信协调

3. **实时数据传输**：
   - 物理AAV将实时数据传回DT服务器
   - 可选：用于监控和可视化
   - 可选：用于在线学习

**执行流程**：

```
每个时间步：

1. 观测环境
   └─> oi,t = 传感器数据 + 邻居信息

2. 推理动作
   └─> ai,t = πθ(oi,t)

3. 执行动作
   └─> 飞行控制指令

4. 信息共享
   └─> 与邻居交换概率图

5. 更新认知
   └─> 贝叶斯更新本地概率图
```

**不需要**：
- 全局状态信息
- Critic网络
- 在线训练（部署后）

#### 2.4.8 持续演化

**反馈循环**：

```
物理执行 → 性能数据 → DT服务器 → 模型优化 → 更新部署
    ↑                                           ↓
    └───────────────────────────────────────────┘
```

**具体步骤**：

1. **数据收集**：
   - 记录实际任务执行数据
   - 包括成功案例和失败案例
   - 记录环境变化和异常情况

2. **性能分析**：
   - 对比预期与实际性能
   - 识别策略弱点
   - 发现新的挑战

3. **模型更新**：
   - 使用真实数据微调模型
   - 可能需要调整奖励函数
   - 改进网络架构（如果必要）

4. **验证测试**：
   - 在DT环境中验证更新的模型
   - 确保性能改进
   - 避免负迁移

5. **重新部署**：
   - 将优化的模型部署到物理AAV
   - 在下一个任务周期使用

**优势**：
- 适应环境变化
- 从失败中学习
- 持续性能提升
- 降低仿真到现实差距

#### 2.4.9 与现有方法的对比

**vs. 传统DRL训练**：

| 特性 | 传统方法 | DT训练框架 |
|------|----------|------------|
| 环境保真度 | 低（简化模型） | 高（物理引擎+真实控制） |
| 训练速度 | 慢（单环境） | 快（并行DT） |
| 仿真-现实差距 | 大 | 小 |
| 持续学习 | 无 | 有 |

**vs. 现有DT方法**：

| 方面 | 现有工作 | 本文框架 |
|------|----------|----------|
| MADRL集成 | 有限 | 完整 |
| 真实验证 | 仅仿真 | 物理飞行 |
| 规模 | 小（<5 AAV） | 大（测试至40 AAV） |
| 任务类型 | 单一 | 协同搜索 |

### 2.5 计算复杂度分析

#### 2.5.1 局部处理复杂度

**单个AAV每步**：

```
O(d² + k)
```

- **d**: GRU隐藏层维度
- **k**: 邻居数量

**组成**：
- GRU计算：**O(d²)**（矩阵乘法）
- 注意力机制：**O(k)**（与邻居数线性）

#### 2.5.2 总局部计算复杂度

**N个AAV，T个时间步**：

```
O(NT(d² + k))
```

**优势**：
- 与总AAV数量N无关（对于单个AAV）
- 只与邻居数k相关
- 邻居数通常很小且有界（如k≤10）

#### 2.5.3 通信复杂度

**每步每个AAV**：

```
O(k)
```

- 只需与k个邻居通信
- 发送Key和Value向量
- 接收邻居的Key和Value

**关键特点**：
- **与总AAV数量无关**
- 仅取决于局部邻居数
- 邻居数通常固定或缓慢增长

**通信量估计**：
假设每个向量维度为d，每步通信量：
```
2 × k × d × (浮点数字节数)
```

如 k=5, d=64, 32位浮点：
```
2 × 5 × 64 × 4 = 2560 字节 = 2.5 KB
```

#### 2.5.4 全局优化复杂度

**Mini-batch梯度下降**：

```
O(BW)
```

- **B**: 批次大小
- **W**: 网络总参数数量

**网络参数规模**：
- Actor网络：~100K-1M参数
- Critic网络：~100K-1M参数
- 总计：**W ≈ 0.2M-2M**

**单次更新时间**：
在GPU上通常为毫秒级。

#### 2.5.5 总体复杂度

```
总复杂度 = O(NT(d² + k)) + O(BW)
```

**训练阶段**：
- 主要瓶颈：全局优化 O(BW)
- 在GPU服务器上执行
- 并行DT加速数据采集

**执行阶段**：
- 主要瓶颈：局部处理 O(d² + k)
- 在AAV机载计算机上
- 实时性要求（通常<100ms）

#### 2.5.6 可扩展性分析

**与集中式方法对比**：

| 方法 | 计算复杂度 | 通信复杂度 | 可扩展性 |
|------|-----------|-----------|----------|
| 集中式 | O(N²T) | O(N²) | 差 |
| SAMARL | O(NT(d²+k)) | O(k) | 优 |

**关键优势**：
1. 局部计算：分布到各AAV
2. 局部通信：仅与邻居
3. 复杂度次线性增长（对于通信）

**实际性能**：
- 测试规模：1-40 AAV
- 可扩展到更大规模（理论上100+）
- 瓶颈：通信网络带宽和延迟

---

## 第三部分：方法论总结

### 3.1 核心创新点

#### 创新1：可扩展观测空间
- **图表示**：规模无关的输入设计
- **FOV机制**：降低维度，提高效率
- **四部分认知图**：全面的局部态势感知

#### 创新2：多目标奖励工程
- **五维奖励**：平衡多个竞争目标
- **权重可调**：适应不同任务需求
- **安全优先**：显式碰撞惩罚

#### 创新3：注意力增强协同
- **多头注意力**：选择性聚焦重要邻居
- **加权聚合**：基于置信度的信息融合
- **动态适应**：处理可变数量邻居

#### 创新4：数字孪生加速
- **并行采样**：Nd个DT同时运行
- **高保真模拟**：真实物理引擎+控制
- **持续演化**：实时反馈闭环优化

#### 创新5：CTDE范式
- **集中训练**：利用全局信息
- **分散执行**：局部观测决策
- **无中心依赖**：鲁棒性强

### 3.2 理论基础

**数学框架**：
- Dec-POMDP（部分可观测马尔可夫决策过程）
- 策略梯度定理
- 贝叶斯推理
- 信息论（熵最小化）

**算法基础**：
- PPO（近端策略优化）
- GAE（广义优势估计）
- 重要性采样
- Transformer注意力机制

### 3.3 实现关键

**训练关键**：
1. 足够的并行DT（建议8-32个）
2. 合理的奖励权重平衡
3. 适当的网络架构（GRU+Attention+MLP）
4. 稳定的优化（Adam，学习率衰减）

**部署关键**：
1. 高效的推理实现（优化模型）
2. 可靠的通信协议（容错机制）
3. 实时的信息融合（贝叶斯更新）
4. 安全的飞行控制（多重保护）

### 3.4 适用场景

**最适合**：
- 大规模AAV集群（10-100+）
- 动态未知环境
- 实时决策需求
- 分布式系统架构

**局限性**：
- 需要较强的计算资源（训练）
- 通信依赖（虽然局部化）
- 环境适应需要时间
- 初期性能可能不如专家规则

### 3.5 与其他方法的定位

```
方法分类图：

传统方法
├─ 规划算法（简单环境，已知地图）
└─ 群智能算法（全局信息，离线优化）

学习方法
├─ 单智能体RL（集中式，可扩展性差）
├─ 独立学习MARL（环境非平稳，性能受限）
└─ SAMARL（CTDE+Attention，本文方法）★
```

**SAMARL定位**：
- 学习方法 → 适应动态环境
- 多智能体 → 处理大规模集群
- CTDE → 训练效率+执行灵活性
- 注意力 → 可扩展性+协同效率
- DT → 仿真-现实无缝迁移

---

## 附录：数学符号表

| 符号 | 含义 |
|------|------|
| **Ω** | 搜索区域 |
| **Lx, Ly** | 网格维度 |
| **cx,y** | 网格中心坐标 |
| **τx,y** | 网格目标存在指示 |
| **Px,y** | 网格目标存在概率 |
| **M** | 目标数量 |
| **No** | 威胁数量 |
| **zk** | 威胁k的位置 |
| **Nu** | AAV数量 |
| **T** | 任务时间步数 |
| **ui,t** | AAV i在时间t的位置 |
| **vi,t** | AAV i在时间t的速度 |
| **ψ** | 最大转向角 |
| **Pr** | 接收信号功率 |
| **Zi,t_x,y** | AAV i对网格的观测 |
| **p, q** | 真检测概率、虚警概率 |
| **Pi,t** | AAV i的概率图 |
| **Ψi,t_x,y** | 对数几率 |
| **ζ** | 目标确认阈值 |
| **dsafe,1, dsafe,2** | 安全距离 |
| **S** | 全局状态空间 |
| **A** | 动作空间 |
| **O** | 观测空间 |
| **oi,t** | AAV i的局部观测 |
| **ρ1, ρ2, ρ3, p4** | 观测四部分 |
| **Ht_x,y** | 网格信息熵 |
| **rcog, rtar, rtime, rcov, rsafe** | 五种奖励 |
| **α, β, λ, μ, γ, η** | 奖励权重 |
| **πθ** | 策略（参数θ） |
| **Vφ** | 价值函数（参数φ） |
| **Aπθ** | 优势函数 |
| **γ** | 折扣因子 |
| **λ** | GAE参数 |
| **ε** | PPO裁剪参数 |
| **qi, li, vi** | Query, Key, Value |
| **zi** | 集合意图 |
| **hi** | 历史信息 |
| **dx** | 缩放系数 |
| **Nd** | 数字孪生数量 |

---

*文档完成日期：2026年1月21日*
*专注于任务建模与方法论的详细中文分析*
